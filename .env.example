# Render provides PORT automatically. Do not normally set PORT yourself there.
PORT=3000

# MongoDB Atlas / external MongoDB
MONGODB_URI=mongodb+srv://phankenpro_db_user:****@game.3cwuc5p.mongodb.net/?appName=game
MONGODB_DB=trumclub0bot

# This legacy callback used a separate database name in the original source.
MONGODB_CALLBACK_DB=X29

# Generate a long random secret for JWT signing.
JWT_SECRET=CHANGE_TO_A_LONG_RANDOM_SECRET

# Optional. Leave empty to disable Telegram integration.
TELEGRAM_BOT_TOKEN=

# Legacy node-telegram-bot-api compatibility flag from original source.
NTBA_FIX_319=1
